# Keep the repo normalized to LF, but check Windows launchers out with CRLF:
# cmd.exe can misparse LF-only batch files (labels and parenthesized blocks).
* text=auto
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.png binary
